<%= form_for(hogar_proyect) do |f| %>
  <% if hogar_proyect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hogar_proyect.errors.count, "error") %> prohibited this hogar_proyect from being saved:</h2>

      <ul>
      <% hogar_proyect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :large_description %>
    <%= f.text_area :large_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
