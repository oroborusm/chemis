<%= form_for(home_proyect) do |f| %>
  <% if home_proyect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home_proyect.errors.count, "error") %> prohibited this home_proyect from being saved:</h2>

      <ul>
      <% home_proyect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :large_description %>
    <%= f.text_area :large_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
